# Atualização forçada em 08/11/2025

# =========================================
# 🌍 TravelMundo IA - Cloud Build (API Node)
# Deploy automático no Cloud Run (us-west1)
# =========================================

steps:
  # 🧩 1. Instala dependências
  - name: 'gcr.io/cloud-builders/npm'
    args: ['install']

  # 🚀 2. Cria imagem Docker e envia para o Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      [
        'build',
        '-t',
        'us-west1-docker.pkg.dev/$PROJECT_ID/travelmundo-api/travelmundo-api:latest',
        '.'
      ]

  - name: 'gcr.io/cloud-builders/docker'
    args:
      [
        'push',
        'us-west1-docker.pkg.dev/$PROJECT_ID/travelmundo-api/travelmundo-api:latest'
      ]

  # ⚙️ 3. Faz o deploy no Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      [
        'run',
        'deploy',
        'travelmundo-api',
        '--image',
        'us-west1-docker.pkg.dev/$PROJECT_ID/travelmundo-api/travelmundo-api:latest',
        '--region',
        'us-west1',
        '--platform',
        'managed',
        '--allow-unauthenticated',
        '--memory',
        '512Mi'
      ]

# ===============================
# 🔐 Permissões & Configurações
# ===============================
serviceAccount: 'travelmundo-api-sa@gen-lang-client-0394942372.iam.gserviceaccount.com'

timeout: 900s  # 15 minutos
options:
  logging: CLOUD_LOGGING_ONLY

